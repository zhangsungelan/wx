<!--index.wxml-->
<view class="container">
  <view>
    <!-- <view wx:for="{{students}}">{{item.name}}</view> -->
    <view>{{name}}</view>
    <button open-type="getUserInfo" bindgetuserinfo="gewxtUserInfo">获取授权</button>
    <open-data type="userAvatarUrl"></open-data>
  </view>
</view>
